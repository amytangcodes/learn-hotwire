<%= turbo_frame_tag "new_discussion_frame" do %>
  <div class="d-flex justify-content-between align-items-center">
    <h1>Discussions</h1>

    <%= link_to 'New Discussion', new_discussion_path, class: 'btn btn-primary' %>
  </div>
<% end %>

<div class="row mt-3">
  <div class="col">
    <div id="discussions">
      <%= render @discussions %>
    </div>
  </div>
</div>
